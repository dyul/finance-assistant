# Finance Assistant 작업 규칙

이 프로젝트는 코딩 초보자가 관리하는 React + TypeScript 기반 Finance Assistant 프로젝트입니다.

1. 모든 작업 결과와 설명은 한국어로 제공합니다.
2. 구현하기 전에 기존 코드와 관련 문서를 먼저 확인합니다.
3. 요청받지 않은 기능, 백엔드 또는 외부 API를 임의로 추가하지 않습니다.
4. 한 번에 하나의 명확한 기능 단위만 수정합니다.
5. 재무 계산에서는 데이터 누락과 실제 값 `0`을 구분합니다.
6. 날짜, 금액, 부호, 입금과 출금의 구분을 명시적으로 처리합니다.
7. 잘못된 데이터를 임의로 추정하거나 `0`으로 대체하지 않습니다.
8. TypeScript 타입을 우선 사용하고 불필요한 `any`를 사용하지 않습니다.
9. 기존 기능과 UI를 요청 없이 삭제하거나 변경하지 않습니다.
10. 작업 후 `npm run lint`와 `npm run build`를 실행합니다.
11. 계산 로직을 변경할 때는 가능한 경우 자동 테스트를 추가합니다.
12. 작업 완료 시 변경 파일, 변경 이유, 검증 결과와 남은 위험을 정리합니다.
13. 파일 삭제, 패키지 설치, `git commit`, `git push`는 사용자 승인 없이 실행하지 않습니다.
14. 대규모 리팩터링은 먼저 계획을 제시하고 사용자 승인을 받은 후 수행합니다.
15. 사용자가 코딩 초보자임을 고려하여 오류와 변경 내용을 쉬운 말로 설명합니다.
