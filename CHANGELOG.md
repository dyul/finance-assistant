# 변경 기록

주요 변경사항은 이 문서에 기록합니다. 현재 package 버전은 `0.1.0-beta.1`이며, 이 문서의 `Unreleased` 표기는 아직 릴리스 tag를 만들지 않았다는 뜻입니다.

## [0.1.0-beta.1] - Unreleased

### 추가

- 다양한 은행·사업자 Excel의 날짜, 금액, 시트와 컬럼 자동 인식
- 자동 인식 실패 시 최대 100행까지 지원하는 직접 설정 흐름
- 전체·월별·카테고리별 현금흐름과 반복 거래 분석
- 최근 수입 추세와 확정 예정 거래를 반영한 향후 3개월 전망
- 보수·기준·낙관 예상 범위와 범위별 현금 위험 분석
- 추천 행동, 데이터 품질·복구 안내, 인쇄·PDF용 분석 리포트
- 온보딩, 실제 업로드 파이프라인을 통과하는 샘플 Excel
- 패키지 추가 없이 UTF-8·UTF-8 BOM과 브라우저 지원 CP949/EUC-KR CSV 입력, quoted field 파싱과 샘플 CSV
- 파일명 기준 예상 범위와 확정 예정 거래 브라우저 저장
- 대표 사용자 흐름, 복합 Excel, 성능·경쟁 상태 회귀 테스트
- SheetJS CE `xlsx@0.20.3`의 Apache-2.0 제3자 라이선스 고지
- 개인정보 보호 원칙을 포함한 Beta 테스트 가이드와 이슈 기록 템플릿
- 익명 Beta 결과 기록, 수동 성공 지표와 실제 Excel 호환성 추적 문서
- 잔액 컬럼이 없는 파일에서 현재 잔액을 직접 입력해 기존 3개월 Forecast·현금 위험·추천 액션에 연결하는 세션 전용 입력 UI
- 파일에서 발견한 유효한 미래 거래를 현재 3개월 Forecast에 자동 반영하고 기본 10건 상세·개별 제외·재포함을 제공하는 UI
- historical 거래를 calendar 월·분기·연도로 집계하는 graph-ready 서비스와 기간 단위 전환 UI

### 변경

- 가로 스크롤 중심의 전망 표를 반응형 월별 카드로 개선
- 최근 거래 행 순서가 아닌 정규화된 거래일로 최근 잔액을 결정
- 불완전한 데이터는 0원으로 추정하지 않고 부분 분석 또는 분석 중단으로 구분
- Excel·CSV adapter를 공통 거래 분석 pipeline에 연결하고 CSV 전용 인코딩·문법·헤더 복구 안내 추가
- 가계부 CSV의 `수입/지출` 방향과 `내역` 설명 헤더를 안전하게 인식하고 `분류`·`하위 분류` 오인식을 방지
- 브라우저의 오늘보다 미래인 원본 거래를 업로드 목록에는 유지하면서 과거 실적·반복 거래·추세·최근 잔액·전망 기준월에서 제외하고, 유효한 현재 기간 거래는 파일 출처 예정 거래로 자동 반영
- 파일 미래 거래와 동일한 stable recurring key가 같은 월에 있으면 파일 확정값을 우선하고 해당 recurring 추정값만 대체
- 대량 거래 자동 분류 표를 50건 단위로 표시하되 범위 밖의 오류·미래 거래는 함께 노출
- 반복 거래 탐지·Forecast 입력은 전체 결과를 유지하면서 상세 분석 표만 기본 10개와 전체 보기·접기로 축약
- 파일 최신 잔액을 우선하고 없을 때만 직접 입력 잔액을 Forecast 시작점으로 사용하며 과거 분석·기준월·저장 payload는 유지
- 장기 월별 목록은 최근 12개 기간을 기본 표시하고, 월×카테고리 주요 지출 표는 기간당 대표 카테고리 한 행으로 축약
- 기간말 잔액은 파일에서 기간 내 마지막으로 확인된 잔액만 표시하고 누락값은 0원으로 추정하지 않음

### 보안

- Excel은 외부 분석 서버로 전송하지 않고 현재 브라우저에서 처리
- `.xlsx`·`.xls`·`.csv`, 최대 10MB 사전 검증과 Excel 파서 지연 로딩
- 취약한 npm registry 배포본 `xlsx@0.18.5`를 SheetJS 공식 CDN 배포본 `xlsx@0.20.3`으로 이전하고 Prototype Pollution·ReDoS 영향 범위와 Excel 회귀 테스트를 재검증
- Vite·PostCSS 빌드 체인의 `nanoid`를 3.3.16에서 안전한 patch 버전 3.3.18로 갱신하고 전체·production 의존성 보안 감사 0건 확인
- Vercel 배포 설정에 `nosniff`, referrer, camera·microphone·geolocation 제한 헤더 선언

### 검증

- Vercel production 배포와 정상 샘플 Excel 분석 성공 확인
- 375px 모바일 핵심 흐름과 production build 재검증
- 44개 테스트 파일의 404개 테스트, lint와 build 통과
- 전체 npm 의존성 보안 감사 0건 확인

### 알려진 문제

- 실제 production URL, 배포 소유자와 피드백 채널은 저장소에 기록되어 있지 않습니다.
- 실제 은행 Excel 전반의 호환성과 실제 사용자 사용성은 Public Beta에서 검증해야 합니다.
- 여러 계좌·파일 통합, 서버 저장, 계정·기기 간 동기화는 지원하지 않습니다.
